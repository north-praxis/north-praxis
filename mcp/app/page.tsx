export default function Home() {
  return (
    <main style={{ fontFamily: 'monospace', padding: '4rem 2rem' }}>
      <p>north-praxis-mcp is running. The MCP endpoint is at /api/mcp.</p>
    </main>
  );
}
